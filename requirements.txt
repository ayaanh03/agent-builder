# Agent framework — we highly recommend the OpenAI Agents SDK (the starter scaffold uses it):
openai-agents>=0.1.0
openai>=1.40
# Prefer Google's Agent Development Kit (ADK)? Use it instead of the two lines above:
#   google-adk>=0.1.0

python-dotenv>=1.0
requests>=2.31
tenacity>=8.2
textual>=0.50
